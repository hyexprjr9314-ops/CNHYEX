# Server-only Vercel environment variables
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Public project key used only to preserve the signed-in user's JWT context for governance RPCs.
SUPABASE_ANON_KEY=

# Optional password reset destination
PASSWORD_RESET_REDIRECT_URL=

# Grade-notice SMTP delivery (server-only)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
